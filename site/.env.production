# Baked into production builds so the deployed site talks to the deployed
# API on any device. Override per build with VITE_API_BASE=<other-url>,
# or per visitor via Settings → Streaming backend (stored in localStorage).
VITE_API_BASE=https://anime-api-rho-three.vercel.app
